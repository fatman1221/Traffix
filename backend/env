# 数据库配置
DATABASE_URL=mysql+pymysql://root:12345678@localhost:3306/traffix

# 模型提供者配置
# 可选值: aliyun, ollama, openai
MODEL_PROVIDER=aliyun

# 模型名称（根据提供者不同而不同）
# 阿里云: qwen-turbo, qwen-plus, qwen-vl-plus 等
# Ollama: llama2, qwen, mistral 等（需要先下载模型）
# OpenAI兼容: 根据你的本地服务配置
MODEL_NAME=qwen-vl-plus

# 阿里云 DashScope 配置（当 MODEL_PROVIDER=aliyun 时使用）
# 在 https://dashscope.console.aliyun.com/ 获取 API Key
DASHSCOPE_API_KEY=sk-5d440c3aec16495484c476135e2b0945

# Ollama 配置（当 MODEL_PROVIDER=ollama 时使用）
# 默认地址: http://localhost:11434
OLLAMA_BASE_URL=http://localhost:11434

# OpenAI 兼容 API 配置（当 MODEL_PROVIDER=openai 时使用）
# 适用于 LocalAI、vLLM 等
OPENAI_BASE_URL=http://localhost:8000/v1
OPENAI_API_KEY=

# JWT密钥配置（用于token生成和验证）
# 重要：如果修改此值，所有已登录用户的token将失效，需要重新登录
# 建议使用一个长随机字符串，例如：openssl rand -hex 32
JWT_SECRET_KEY=your-secret-key-change-in-production

